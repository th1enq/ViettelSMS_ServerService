package main

const (
	DefaultConfigFilePath = "configs/config.dev.yaml"
)

func main() {
	// config, err := config.NewConfig(DefaultConfigFilePath)
	// if err != nil {
	// 	panic("failed to load configuration: " + err.Error())
	// }

}
